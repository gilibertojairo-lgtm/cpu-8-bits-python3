from setuptools import setup, find_packages

setup(
    name="cpu8bit",
    version="0.1",
    description="CPU virtual de 8 bits en Python",
    author="TuNombre",
    packages=find_packages(),
    python_requires=">=3.8",
)
